# OpsPilot 代码审查报告

**审查时间**：2026-09-14
**审查范围**：`C:/Users/25791/Desktop/OpsPilot`（commit `c12e21e`）
**审查方式**：全量读码 + 实机 API 验证 + 静态可达性分析

---

## 0. 总结论

| 维度 | 评价 | 说明 |
|---|---|---|
| **后端工程质量** | **A** | 6291 行，注释记录真实踩坑，无 mock，26 工具真实注册 |
| **前后端契约一致性** | **B−** | 27 路由 vs 13 前端调用；**1 处真实参数错配（丢数据）** |
| **前端实况路径完成度** | **C+** | 换掉了演示数据，但**仍有 13 个死按钮 + 3 处硬编码** |
| **整体** | **B** | 骨架是真实的 Agent，不再是 PPT，但"最后一公里"没走完 |

**一句话**：上一轮"去 mock"方向正确、执行到位；但**"没有写死的数据"≠"交互可用"** —— 现在的问题从"假数据"变成了"真数据 + 点了没反应的按钮"。

---

## 1. 必须修的缺陷（P0）

### P0-1 拒绝理由被静默丢弃（真实数据丢失）

**位置**：`frontend/src/api/client.ts` L68-70 vs `backend/ops_pilot/server/app.py` L359-361

```ts
// 前端（client.ts L68-70）
export const rejectApproval = (id: string, reason: string) =>
  request(`/api/approvals/${id}/reject?reason=${encodeURIComponent(reason)}`, ...)
```
```python
# 后端（app.py L359-361）
@app.post("/api/approvals/{approval_id}/reject")
def reject(approval_id: str, rejected_reason: str = "User rejected the action."):
```

**参数名不匹配**：前端发 `reason`，后端只认 `rejected_reason`。

**实机验证证据**（后端 :8791，真实请求）：
```
--- 前端写法 ?reason=测试原因 ---
status: rejected, rejected_reason: "User rejected the action."   ← 理由丢失，落默认值
--- 后端签名 ?rejected_reason=测试原因 ---
status: rejected, rejected_reason: "测试原因"                    ← 正确
```

**影响**：`RealStream.tsx` L104 传的 `reason: "用户拒绝"` 永远不会落库。审批记录里拒绝理由恒为英文默认串 —— 运维审计场景下这是实质缺陷（"谁为什么拒绝了高危操作"答不出来）。

**修法**（一行）：
```ts
request(`/api/approvals/${id}/reject?rejected_reason=${encodeURIComponent(reason)}`, ...)
```

---

### P0-2 `ApprovalCard` 内有 4 个死按钮，导致"双份批准按钮"

**位置**：`frontend/src/components/cards.tsx` L138-143

```tsx
<div className="acts">
  <button className="b">查看命令</button>              {/* 死 */}
  <button className="b go"><Icon.check/>批准执行</button> {/* 死 */}
  <button className="b no">拒绝</button>                {/* 死 */}
  <button className="b warn">修改方案</button>          {/* 死 */}
</div>
```

**证据链**：
1. `RealStream.tsx` L4 导入 `ApprovalCard` → 实况路径可达
2. `ApprovalCard` 内部 4 个按钮**全部无 `onClick`**
3. `RealStream.tsx` L100-107 不得不在**卡片外部**另写一份真按钮：
```tsx
<div className="acts">
  <button className="b go" onClick={() => approve.mutate(approval.id)}>批准执行</button>
  <button className="b no" onClick={() => reject.mutate({...})}>拒绝</button>
</div>
```

**影响**：界面上出现**两组**"批准执行/拒绝"，上面那组（卡片内）点了没反应。用户会先点错。同时"修改方案"按钮完全没有实现。

**修法**：给 `ApprovalCard` 加可选回调 props（`onApprove/onReject/onViewCommand/onModify`），由 `RealStream` 传入；删掉外部的重复按钮组。

---

### P0-3 `ContextRing` 上下文占用环是硬编码的假数据

**位置**：`shell/Center.tsx` L16-17（默认参数）→ L130；`App.tsx` L16/L37/L116

```tsx
export function ContextRing({ pct, used = "48.6K", total = "128K", hit = 91,
                              turnsLeft = 26, condensed = false }) {
```

**证据**：
1. 后端**有**真实端点：`GET /api/tasks/{id}/context-usage`（`app.py` L388-394，返回 `context_used_percent` + `ring_state`）
2. 前端**零调用** —— `client.ts` 里连 `getContextUsage` 函数都没写
3. `ringPct` 从 `App.tsx` L116 传入，值来自 `STATE_META` 的**静态表**（仅 `"06"` 写了 `ringPct: 78`，其余 undefined → 用默认值）
4. 悬浮提示里的 `21.2K · 6.8K`、`18.9K / 未命中 2.3K`、`26 轮` **全是写死的字面量**

**影响**：这是"上下文即将溢出"的唯一预警 UI，在真实长会话里显示的是一个**固定不变的假数字**。

**注**：这算是上一轮的遗漏 —— `context-usage` 端点和 `usage.py`（66 行，含 `normalize_usage`/`ring_state`）都写好了，前端没接。

---

## 2. 应该修的缺陷（P1）

### P1-1 实况路径下共 13 个死按钮

| 文件 | 数量 | 行号 | 实况可达性 |
|---|---|---|---|
| `components/cards.tsx` | 7 | 139-142, 181-183 | ✅ RealStream 引用 |
| `shell/Center.tsx` | 2 | 102, 104 | ✅ 实况 Composer |
| `shell/TopBar.tsx` | 2 | 19, 47 | ✅ 全局 |
| `shell/MenuBar.tsx` | 1 | 66 | ✅ 全局 |
| `shell/Sidebar.tsx` | 1 | 36 | ✅ 全局 |

**具体清单**：
- `cards.tsx` L139-142：查看命令 / 批准执行 / 拒绝 / 修改方案（见 P0-2）
- `cards.tsx` L181-183：按建议整改 / 导出报告 / 查看完整执行记录（`SummaryCard`，**目前无实况调用方**，属潜在）
- `Center.tsx` L102：**添加文件 / 选择服务器 / 选择数据库 / 添加上下文** —— 4 个按钮一行，实况 Composer 里点了全无反应
- `Center.tsx` L104：**使用终端**
- `TopBar.tsx` L19：任务标题按钮；L47：设置
- `MenuBar.tsx` L66：菜单项
- `Sidebar.tsx` L36：`Leaf`（数据库树的 Tables/Views/Query Console）

**判断**：TopBar/MenuBar 的壳按钮优先级低（产品形态未定）；**`Center.tsx` L102-104 的 5 个按钮优先级高** —— 它们是"让 Agent 干活"的主要入口，现在纯装饰。

---

### P1-2 `mock/` 已正确隔离，但 `Stream.tsx` 仍是死重量

**验证结论（本轮实测）**：
- 全 `src/` 中引用 `mock/` 的**仅 1 个文件**：`components/Stream.tsx` L1 (`from "../mock/streams"`)
- `Stream.tsx` 仅在 `Center.tsx` L285 渲染，而该分支要求 `live === false`
- `live` 由 `viewState.ts` 的 `isLiveMode()` 决定 = `!isDesignPreview()`，即**只有 `?state=NN` 才为 false**

**判定**：隔离干净，符合"设计态预览"定位。**但"状态 01"的快速提问按钮（`Center.tsx` L274-281"检查这台服务器为什么 CPU 很高"等）也只在设计态出现** —— 实况模式下没有快捷入口，这是个体验缺口。

---

### P1-3 `Sidebar` 的"新建连接/新建项目"只弹提示不落库

**位置**：`shell/Sidebar.tsx` L224-231

```tsx
onClick={() => { setNav("servers"); setNotice("选择左侧任一服务器，在中间输入运维指令即可开始…"); }}
onClick={() => { setNav("projects"); setNotice("在中间输入「新建项目」并给出名称与路径，Agent 会通过 API 落库。"); }}
```

**证据**：`client.ts` 中 `createServer`、`createDatabase`、`createProject`、`testServer` **四函数零引用**（本轮静态审计）—— 即"新建"只能走 Agent 对话，UI 本身无表单。

**判定**：这是**有意的降级设计**（把写操作交给 Agent），不算 bug，但按钮文案叫"新建连接"却只弹提示，**期望管理有偏差**。建议改文案或在提示里明确"请用对话创建"。

---

## 3. 值得肯定的部分

| 项 | 证据 |
|---|---|
| **后端零 mock** | `ops_pilot/` 39 文件 6291 行，无 TODO/mock 残留 |
| **踩坑有记录** | `runner.py` 注释精确到行号（SDK 1.47 API 漂移、analyzer 必须先装、FinishAction 过滤、FIFO 收口） |
| **契约自检** | `app.py` L68-79 `_warn_level_drift()` 启动即告警工具清单与等级表漂移 |
| **孤儿对账** | `app.py` L87-101 重启后把 `running` 改 `interrupted`，不假装还在跑 |
| **凭据边界** | Action schema 只带 `server_id`；`app.py` L507 注释记录了"身份字段必须最后落定"的真实坑 |
| **错误码体系** | `diagnostics/codes.py` 434 行 50 错码，`not_implemented()` 强制标注未实现 |
| **测试** | 193 passed（隔离环境实测，本轮复现） |
| **前端去 mock 到位** | 右栏三面板已换真实数据源；`Sidebar` 无 `?? SERVERS` 回退；`Center` 无 `?? "hk-ubuntu"` |

---

## 4. 未实现清单（如实）

| # | 项 | 状态 | 影响 |
|---|---|---|---|
| 1 | Tauri 桌面壳 | `API_BASE=""` + 无 Python sidecar | 独立分发不可用 |
| 2 | 上下文用量环 | 端点就绪，前端未接 | 预警 UI 是假的（P0-3） |
| 3 | 审计查询 UI | `/api/audit` 端点就绪，前端零调用 | 审计数据只能 curl 看 |
| 4 | 任务历史 UI | `useTaskList` 零调用 | 侧栏"任务历史"无内容 |
| 5 | 权限档位查询 | `usePermission` 零调用 | 档位状态不持久显示 |
| 6 | 数据库树 | 端点就绪，侧栏 `Leaf` 是静态的 | 表/视图不展示 |
| 7 | 服务部署脚本 | `run_change_script` 通道就绪 | 脚本需用户提供 |
| 8 | 待审批会话持久化 | 进程内内存 | 重启后不可恢复 |

---

## 5. 修复优先级建议

```
P0（丢数据/误导，必须修）
  1. client.ts reject 参数名     —— 1 行
  2. ApprovalCard 死按钮 + 去重   —— 加 props，删 RealStream 外部按钮组
  3. ContextRing 接真实端点      —— client.ts 加函数 + hooks 加 hook + Center 传值

P1（可用性）
  4. Center.tsx L102-104 五个按钮 —— 接 "选择服务器" 打开侧栏 / "添加文件" 文件选择
  5. Sidebar "新建连接" 文案澄清

P2（壳装饰，可延后）
  6. TopBar/MenuBar 死按钮
  7. mock 快提问按钮也在实况态提供

未实现（需产品决策）
  8. Tauri sidecar / 审计 UI / 任务历史 UI
```

---

## 6. 复核命令

```bash
# 后端测试（193 passed）
cd backend && "C:/Users/25791/.workbuddy/binaries/python/envs/opspilot/Scripts/python.exe" -m pytest -q

# 类型检查 + 构建
cd frontend && NODE_OPTIONS= node node_modules/typescript/bin/tsc -b --force
cd frontend && NODE_OPTIONS= node node_modules/vite/bin/vite.js build

# 复现 P0-1（reject 参数错配）
cd backend && python -m uvicorn ops_pilot.server.app:app --port 8791
# 另开：
#   POST /api/approvals            → 建单
#   POST /api/approvals/{id}/reject?reason=X        → 落默认值（丢数据）
#   POST /api/approvals/{id}/reject?rejected_reason=X → 正确落库
```
